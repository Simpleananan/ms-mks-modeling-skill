# Legacy design documents

These files preserve the original v0.1–v0.3 design/product/retrieval documentation for traceability. They are **historical design assets, not runtime instructions**. Some paths, installation assumptions, privacy-file references, and retrieval CLI details are superseded by the root-level `SKILL.md`, `README.md`, `MIGRATION.md`, and current files under `references/`.

They are retained so the v0.4 upgrade does not erase earlier reasoning, product scope, validation ontology, or retrieval design history.
